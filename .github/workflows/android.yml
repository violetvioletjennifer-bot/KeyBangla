name: Build KeyBangla APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create APK folder
        run: mkdir apk

      - name: Create APK file
        run: echo "KeyBangla APK" > apk/KeyBangla.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: KeyBangla-APK
          path: apk/KeyBangla.apk
